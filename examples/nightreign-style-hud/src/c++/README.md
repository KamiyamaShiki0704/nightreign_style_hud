# Legacy C++ Notes

This folder is kept as historical reference only. The active DLL entry point and
HUD implementation are the Rust files in `src/`.

Do not expect `dllmain.cpp` to participate in the current Cargo build.
